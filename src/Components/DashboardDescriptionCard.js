import React from 'react';
import {Card, CardHeader, Typography, IconButton, CardContent, CardActions} from '@material-ui/core';
import {Link} from 'react-router-dom';
import './DashboardDescriptionCard.css';
import { ArrowRight } from '@material-ui/icons';

/*class DashboardDescriptionCard extends React.Component {
    render() {
        return (
            <Card boxShadow={3}>
                <CardHeader
                    title={this.props.title}
                />
                <CardContent>
                    <Typography variant="body2" color="textSecondary" component="p">
                        {this.props.description}
                    </Typography>
                </CardContent>
            </Card>
        )
    }
}*/

export default DashboardDescriptionCard;